# /build — run the site build and report

Run the full site generator and report the result.

```bash
python3 scripts/build_site.py
```

Report:
- How many pages were rendered
- Any errors or schema validation failures
- Whether the build succeeded or failed
