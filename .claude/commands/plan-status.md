# /plan-status — show PLAN.md progress

Read `PLAN.md` and report:
- How many tasks are checked (`- [x]`) vs unchecked (`- [ ]`)
- Which phase is currently active (first phase with unchecked items)
- The next unchecked task

Keep the report under 10 lines.
