# Python bytecode cache (from running scripts/ locally)
__pycache__/
*.pyc

# Claude Code — ignore internal state files, track user config (commands, settings)
.claude/*
!.claude/settings.json
!.claude/commands
